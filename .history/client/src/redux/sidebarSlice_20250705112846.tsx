import { createSlice } from "@reduxjs/toolkit"

const sidebarSlice = createSlice({
    name: "sidebarSlice",
    initialState: {
        open: false
    },
    reducers: {
        openSidebar: 
    }
})